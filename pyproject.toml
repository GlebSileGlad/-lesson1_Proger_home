[tool.poetry]
name = "testsite"
version = "0.1.0"
description = ""
authors = ["Hleb <silentium.90d@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"
flake8 = "^4.0.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
